<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.keklist">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".MainListActivity" />
        <activity android:name=".AdActivity"/>
        <activity android:name=".PersonalAd"/>
        <activity android:name=".FormActivity"/>
        <activity android:name=".UpdateFormActivity"/>
        <activity android:name=".SearchActivity"/>
        <activity android:name=".CabActivity"/>
        <activity android:name=".SignUpActivity"/>
        <activity android:name=".StartListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>